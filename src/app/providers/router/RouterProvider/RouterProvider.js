import { Suspense } from "react";
import { Routes } from "react-router";

export const RouterProvider = () => (
    <Suspense>
        <Routes>
            {/* TODO ROUTE */}
        </Routes>
    </Suspense>
);

